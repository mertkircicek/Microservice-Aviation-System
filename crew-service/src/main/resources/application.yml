server:
    port: 8082

spring:
    application:
        name: crew-service
    datasource:
        url: jdbc:sqlserver://localhost:1433;databaseName=AviationDB;encrypt=true;trustServerCertificate=true
        username: sa
        password: MRT200124mrt
        driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true

eureka:
    client:
        service-url:
            defaultZone: http://localhost:8761/eureka/
